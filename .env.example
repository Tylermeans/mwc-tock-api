# Copy to .env (gitignored — NEVER commit the real file; this repo is public).
# n8n Cloud API key for midwest.app.n8n.cloud — used to import/manage the
# reservation-board workflow via the n8n REST API.
N8N_API_KEY=
